<h1><%= t('.tricky') %></h1>

<p><%= link_to t('.create_new'), new_admin_test_path %></p>

<table class="table">
  <thead>
    <tr>
      <th scope="col"><%= t('.title') %></th>
      <th scope="col"><%= t('.level') %></th>
      <th colspan="3" scope="col"><%= t('.actions') %></th>
      <th scope="col"><%= t('.questions') %></th>
    </tr>
  </thead>

  <%= render @tests %>
</table>
